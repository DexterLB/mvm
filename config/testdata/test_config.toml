file_root = "/foo/bar"

[library]
    database = "foosql"
    database_dsn = "bar"

[importer]
    buffer_size = 50
    
    [importer.osdb]
        max_requests = 3
        max_per_request = 199
        username = "foo"
        password = "bar"
    [importer.imdb]
        max_requests = 16
